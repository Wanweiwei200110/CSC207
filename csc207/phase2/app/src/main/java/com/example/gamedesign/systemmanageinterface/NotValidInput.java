package com.example.gamedesign.systemmanageinterface;

/** NotValidInput interface for checking username and password. */
interface NotValidInput {

  /**
   * checking the input
   *
   * @param validateResult a string of validate result
   */
  void notValidInput(String validateResult);
}
